// The bare /bands index. The directory grid itself is rendered by the layout,
// so this route has no content of its own — it just gives the grid a real URL
// and the profile overlay's back/close links a destination to return to.
export default function BandsIndex() {
  return null;
}
